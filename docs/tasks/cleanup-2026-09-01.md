---
status: design          # design | coding | review | fix | blocked | done
branch: fix/cleanup-2026-09-01
---

# 任务：2026-09-01 工程清理复核（审查后修订稿）

> **⚠️ 本稿为 2026-09-01 评审后的修订版**。原稿 4 条"清理对象"经取证后：3 条事实不成立、1 条方向颠倒。修订后本任务**从执行清单改为"复核 + 记录结论"**，不擅自删改。

## 交接指令（复制给 Cursor，不用手敲）

> `docs/tasks/cleanup-2026-09-01.md` · `fix/cleanup-2026-09-01`

```bash
git checkout master && git pull && git checkout -b fix/cleanup-2026-09-01
```

> 复制本文件为 `docs/tasks/cleanup-2026-09-01.md`，作为 Claude Code × Cursor 的**唯一交接载体**。
> 禁止在聊天里互贴长方案/状态；另一方 `git pull` 后读此文件。

- **主责（当前阶段）**：Claude Code（design） → Cursor（verify 复核）→ Claude Code（review）→ 结论归档

## 目标

对 2026-09-01 首轮评审提出的 4 条"工程卫生问题"，**逐条取证后给出处置结论**。首轮建议（清 `__pycache__` / `.pytest_cache` / 去 BOM / 重命名乱码 fixture）经取证后**大部分不成立**，本任务把它们固化进文档，避免执行器误删。

## 取证结论（Claude Code 已核，2026-09-01）

| 原建议 | 取证结果 | 处置 |
|---|---|---|
| 1. 清 `connector-csv-local/__pycache__` 跟踪 | `git ls-files \| grep -c connector-csv-local/__pycache__` = **0**，未被跟踪；`.gitignore` L3 已含 `__pycache__/`，`git check-ignore` 确认已忽略 | ✅ **不成立，非任务**。磁盘目录存在但从未入库，无需处理 |
| 2. 清根部 `.pytest_cache` 跟踪 | `git ls-files \| grep -c pytest_cache` = **0**，未被跟踪；`.pytest_cache/.gitignore` 内 `*` 自忽略 | ✅ **不成立，非任务**。无跟踪需清 |
| 3. 去 `backend/requirements.txt` BOM | BOM 确在（首字节 `EF BB BF`）；**但 commit `fdc2847` 的主题就是"save … with BOM"，是加 BOM 而非残留**；且该文件**含大量中文注释**（OCR/RapidStruct 说明行），BOM 很可能是为了 Windows 默认 codepage 下 pip 正确解析中文 | ⚠️ **方向颠倒，不执行**。BOM 是有意加码；去除反而可能让中文注释在 GBK/codepage 下解析失败。**保持现状**。若未来要改，需先确认"中文注释 + 无 BOM"在 Windows 默认 pip 可解析，再单独立项 |
| 4. 重命名"GBK 乱码 `���.csv`"为 ASCII | 全仓被跟踪的非 ASCII fixture 文件名是 **`假设参数.csv`**（UTF-8 中文，正常），被 `extensions/profit-assumptions/handler.py` + `finance-pl/` 等 **6 处**引用。不存在"GBK 脏数据.csv" | ✅ **不成立，非任务**。`假设参数.csv` 是正常命名且被引用，**禁止重命名** |

## 边界 / 不做

- **不执行** 原 4 条中任何实际的 `git rm` / `git mv` / 去 BOM
- **不新增** pre-commit 钩子 / 工具
- 本任务产出 = **结论文档 + 验证命令**（供后续任何人不再误判）

## 验收

- [ ] `docs/tasks/cleanup-2026-09-01.md` 本文档已列为"复核结论版"（frontmatter/正文都已改）
- [ ] 运行下列验证命令，输出符合预期、与上表一致：
  ```bash
  git ls-files | grep -c "connector-csv-local/__pycache__"     # 期望 0
  git ls-files | grep -c "pytest_cache"                          # 期望 0
  xxd backend/requirements.txt | head -1                         # 期望 EF BB BF（BOM 保持）
  git ls-files | grep "假设参数.csv"                             # 期望命中（正常引用）
  ```
- [ ] git 仓库无新改动（本任务不改代码，只确认现状 + 归档结论）

## 方案

### 1. 结论归档位置

结论已写入本文档。后续任何人（含 CI / review）援引"清 __pycache__ / 去 BOM / 重命名乱码"时，应先读本文件确认**这些是已否定的建议**。

### 2. 若未来真要动"BOM 去留"（单独立项前置）

- 确认 `backend/requirements.txt` 中文注释在 **无 BOM** 且 Windows 默认 codepage（GBK）下 `pip install -r` 仍成功
- 或在 `install.bat` / 文档中约定"以 UTF-8 打开该文件"
- 在此之前**保持 BOM**

### 3. 验证命令（Cursor verify 阶段跑）

见「验收」一节 5 条。

## Review notes（Claude Code 填，review 阶段，只读不改代码）

（待 review 时填）

## 进度 log（谁改谁 append，一行一条）

| 日期 | 阶段 | 负责 | commit | 说明 |
|---|---|---|---|---|
| 2026-09-01 | design | Claude Code | (待 commit) | 复核修订：原 4 条经取证，3 条不成立、1 条颠倒；改为结论归档不修改 |