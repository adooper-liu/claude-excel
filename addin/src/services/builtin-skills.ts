/** Built-in skill bodies. Kept as TS so webpack never has to parse SKILL.md. */
import { skillCreateGuide } from "./skill-create-guide";

export const reconcileSkill = [
  "# reconcile",
  "",
  "两表对账。精确匹配，结果只写新表。",
  "",
  "## 何时用",
  "",
  "用户要把两份名单/订单/流水对上：谁两边都有、谁只有一边、谁键相同但金额等字段不一致。",
  "",
  "## 步骤",
  "",
  "1. `inspect_workbook` — 看有没有 Table、表头叫什么。空工作簿或没有带表头的表：只回短句让界面出勾选框，不要列举样例表头、不要编造数据、也不要报错结束。",
  "2. 还不是 Table → `ensure_table`（含表头的区域）。后续必须用返回的 `name`（中文表名可能变成 `T_系统订单表`）。",
  "3. `inspect_table` 确认键列名",
  "4. `reconcile_tables`，传入 `leftTable` `rightTable` `keys`",
  "",
  "键列有空格/大小写/前导零差异时：先看 `inspect_table` 的 sampleRows 判断差异大小。差异小（空格/大小写）对账会自动去空格；差异大（前导零、缩写、别名对不上）提示用户先 /整形 归一键，或换键列，不要硬对。",
  "",
  "不要覆盖源表。不要用模糊匹配。空键不会互配。不要用 `write_to_sheet` 手写对账结果。",
  "",
  "## 结果列",
  "",
  "`status`（matched / left_only / right_only / conflict）、`key`、`left_*`、`right_*`、`conflict_columns`",
  "",
  "## conflict 处理",
  "",
  "conflict（键相同但字段不一致）把 left_*/right_* 的差异摆出来，交给用户判断：以左表为准 / 以右表为准 / 人工核对。列选项，不替用户选。报告冲突行数和涉及列名。",
].join("\n");

export const reshapeSkill = [
  "# reshape",
  "",
  "把脏表收成能算的新表。源表只读。",
  "",
  "| op | 用户怎么说 | 结果 |",
  "|---|---|---|",
  "| `dedupe` | 按订单号去重 | 键相同只留第一行 |",
  "| `unpivot` | 反透视 / 转长表 | 宽表变长表（属性 / 值） |",
  "| `split` | 把标签按逗号拆开 | 一列拆成 `列_1` `列_2` … |",
  "| `coerce` | 把金额转成数字 | 非法值变空 |",
  "| `coerce_columns` | 识别列格式并写新表 | 先 inspect_table 看 columnHints；单号 id_text→@，金额→数字，时间 datetime 原样 |",
  "| `project` | 按位置映射/合并列 | 选列或 merge 多列，赋新列名；`headerless:true` 时首行也是数据 |",
  "| `flatten_header` | 双层/合并表头拍平 | 大类+子列→一行表头（如 首次扫描时间_文本）；`sheetName`+`range`+`headerRows:2`；不需 ensure_table |",
  "",
  "除 `flatten_header` 外必须先是 Excel Table（`ensure_table`）。双层表头禁止 read_range 后 write_to_sheet。空工作簿或没有带表头的表时，只回短句让界面出勾选框，不要报错结束。不跨文件合并。从网址/ERP 取数走 /取数。",
  "",
  "用户说法不在上面任何 op 的「怎么说」列时（如转置、行转列、合并表），列出 2–3 个候选 op 让用户选，不要硬套一个。",
].join("\n");

export const calculateSkill = [
  "# calculate",
  "",
  "用还活着的公式算。源表只读；汇总值禁止写死。",
  "",
  "| op | 用户怎么说 | 写出什么 |",
  "|---|---|---|",
  "| `lookup` | 按订单号把金额匹配过来 | 新表里 `INDEX` + `MATCH`（精确匹配，不依赖 XLOOKUP） |",
  "| `sumifs` | 按类别求和 | 新表里 `SUMIFS`，合计会随源表变 |",
  "| `fix_ref` | 修 #REF! | 新表去掉公式里的 `#REF!` 参数 |",
  "",
  "必须先是 Excel Table（`ensure_table`）。空工作簿或没有带表头的表时，只回短句让界面出勾选框，不要报错结束。透视走 /透视（create_pivot），不要用 SUMIFS 假装透视。",
  "",
  "## 写公式时",
  "",
  "- 查找默认 `INDEX`/`MATCH`，分类汇总默认 `SUMIFS`，容错用 `IFERROR`。不要把心算或脚本算好的数字贴进单元格。",
  "- 日期区间求和（近 30 天金额）：`SUMIFS(金额列, 日期列, \">=\"&起, 日期列, \"<=\"&止)`；日期列用真实列字母，条件里日期用 DATE() 或引用单元格，不写裸字符串。",
  "- 多条件求和（按类别+仓库）：`SUMIFS(金额列, 类别列, 条件1, 仓库列, 条件2)`。",
  "- 分母可能为 0 时写成 `IF(分母=0,\"\",分子/分母)`，不要直接相除。",
  "- 工作表名不是纯英文下划线时，跨表引用必须加单引号：`='假设输入'!$B$5`。",
  "- 合并单元格只写左上角。",
  "- 写完用 `inspect_formulas` 抽查 2–3 个格子，确认引用的是当前表头，而不是你脑补的列名。",
  "- `#REF!` 用本技能的 `fix_ref`；其它错误（`#DIV/0!` `#N/A` `#NAME?`）先 `scan_formula_errors` 报告位置，不要瞎改。",
].join("\n");

export const craftSkill = [
  "# 规范",
  "",
  "把当前表做成能给人用的活表。先看公式和错误，再用现有格式工具上色。不覆盖源表数值。",
  "",
  "## 何时用",
  "",
  "用户说规范一下、财务模型格式、检查公式、给输入格上色、加填写说明、数字显示不对。对账、去重、反透视仍走对应工具，不要另造格式技能。",
  "",
  "## 步骤",
  "",
  "1. `inspect_workbook`。空表或没有表头：只回短句让界面出勾选框，不要编数据。",
  "2. `inspect_formulas`（有 Table 就传 tableName）或 `scan_formula_errors`。同时看公式原文和算出来的值。错误只报告位置；`#REF!` 交给 `/计算` 的 fix_ref，其它错误不要瞎改。",
  "3. 该汇总或按键匹配的，走 `/计算`（`calculate_table`）。汇总值必须是活公式：`INDEX`/`MATCH`、`SUMIFS`。禁止把心算结果写进格子，禁止默认 XLOOKUP。",
  "4. 用 `format_range` 按格上色（不要发明新工具）：手工输入的数字 `color=#0000FF`；同表公式 `color=#000000`；公式里带 `!` 的跨表引用 `color=#008000`；用户标明的关键假设区 `bgColor=#FFFF00`。上色前先用一句话告诉用户这套颜色语义（蓝=可改输入、黑=公式、绿=跨表引用、黄=关键假设），用户同意或已懂再上色，不要默默上色。表头可 `bold`+`hAlign=center`+`border=thin`；冻结首行 `freezeRows=1`。下拉列表用 `data_validation` type=list。",
  "5. 金额 `numberFormat=¥#,##0.00;¥(#,##0.00);-`；比例 `0.0%`（格子里存 0.15，不要写 15）；年份当文本，不要千分位。",
  "6. 假设放在单独格子，公式引用它，禁止 `=B5*1.05` 这种魔数。旁边一格写来源（用户给的就写「用户提供」）。",
  "7. 改已有表：先靠 inspect_formulas 认出输入格，只改那些格，已有公式不要碰。给别人填的空表才加「填写说明」和一行示例；编辑别人的文件时不加示例行。",
  "8. 合并单元格只写左上角。用户点名的表头和公式照做，不要另起一套结构。",
].join("\n");

export const skillCreatorSkill = [
  "# skill-creator",
  "",
  "把用户工作流做成可安装的 SKILL.md。不要改工作簿。",
  "",
  "## 优先消费上一轮的拆解交接块",
  "",
  "若对话里上一轮有 `/拆解` 输出的 ```【拆解交接】``` 代码块，直接按它起草，不要再问「流程是什么」：",
  "",
  "- 🟢 步骤 → 对应的真实算子，参数用当前工作簿的真实表头。",
  "- 🟡 判断点 → 写进 SKILL.md 的「口径选项」，不替用户拍板。",
  "- 验证锚点 → 用作试跑检查。",
  "",
  "只补缺的：口令（slash）、结果长什么样、试跑口令。没有交接块才走下面的「收意图」流程。",
  "",
  "## 先看用户处在哪一步",
  "",
  "- 从零说「做成技能」→ 先收意图，再起草。",
  "- 已经贴了一份 SKILL.md / 上次没装上 → 改草稿，不要重写无关部分。",
  "- 只要试跑或改触发语 → 只动 description 和试跑口令。",
  "- 用户说不用试、直接写 → 起草后给出代码块即可。",
  "",
  "## 收意图（本轮对话里有的先用，缺的最多问四件）",
  "",
  "1. 做成之后，模型要替用户干什么？",
  "2. 用户可能怎么开口？（写进 description，不要只写在正文）",
  "3. 结果长什么样？（新表名、要保留活公式还是只要值）",
  "4. 要不要用当前工作簿的真实表头走一遍？能客观核对的流程（对账、整形、写公式）建议走；写文案类的不必。",
  "",
  "若现有对账、整形、计算、透视、假设、取数、调研、规范已经能做，用中文告诉用户怎么开口，停止新建。不要让用户先背斜杠清单。",
  "",
  "## 起草 SKILL.md",
  "",
  "- 本回合创建时只读：`inspect_workbook` / `inspect_table` / `inspect_formulas`。禁止在创建回合里 ensure_table、reconcile、reshape、calculate、write、format。",
  "- YAML：`name`、`description`、`slash`。slash 用中文短命令，不能占用内置名（含 skill-creator、skill）。",
  "- description 写「做什么 + 什么时候用」，把用户可能说的话写进去，宁可稍宽，不要写得太窄导致唤不起来。",
  "- 正文按下面「编排 Office JS」写：每步点名现有算子，参数来自真实表头。不要发明工具名。",
  "- 说明为什么要某一步（例如「先 inspect 是为了对着真实表头，而不是猜列名」），少用空洞的必须。",
  "- 短：能一屏看完。不要把通用纪律再抄一遍。",
  "",
  skillCreateGuide(),
  "",
  "## 试跑口令",
  "",
  "附 2–3 句真人会说的话。用当前表头在脑子里走一遍：缺列、缺工具、会覆盖源表，就改草稿再给。用户说跳过试跑则跳过。",
  "",
  "回复里用一个 markdown 代码块给出完整 SKILL.md。",
  "",
  "## 安装引导",
  "",
  "给完 SKILL.md 代码块后，告诉用户怎么装：任务窗格输入 `/` 后点「安装外部技能」→ 上传 SKILL.md 文件，或拖放 / 粘贴内容。装完用 2–3 句试跑口令验证。",
].join("\n");

export const pivotSkill = [
  "# 透视",
  "",
  "按当前表头做 Excel 透视表。源表不改。",
  "",
  "1. `inspect_workbook`，表头以返回为准，不要猜列名。",
  "2. 还不是 Table → `ensure_table`，后续用返回的 name。",
  "3. `create_pivot`：rows 用「要按什么分类」的列（可多级，rows 传数组）；values 用「要加总/计数」的列（aggregation 默认 sum，计数用 count，平均用 average）；列字段、筛选可选。字段名必须来自 inspect 的真实表头。",
  "4. 汇报新表名。不要用 write_to_sheet 手搓透视结果。",
].join("\n");

export const assumeSkill = [
  "# 假设",
  "",
  "就地改输入格，公式不动。Excel 自己重算利润表/资产负债表等下游。",
  "",
  "1. `inspect_workbook` 再 `inspect_formulas`，从 inputSample 认出输入格。",
  "2. 只用 `write_inputs` 写入用户给的新数字。目标格若是公式，工具会拒绝，不要改用 write_to_range。",
  "3. 再 `inspect_formulas` 或 `scan_formula_errors`，报告用户关心的结果格新旧值、下游是否还报错。",
  "4. 不要复制整张模型到新表。不要把密码或 ERP 账号写进格子。",
  "5. 用户要对比两套假设（乐观/悲观、改税前后）时：先把输入区复制到新表或新列，再改其中一套，不覆盖原假设；对比结果若要落表，用 /计算 或 /透视 出。",
].join("\n");

export const fetchSkill = [
  "# 取数",
  "",
  "把网址里的**结构化表行**写进工作簿。不是市场调研、政策解读或竞品长文——那些走 /调研。密码不准出现在对话或工具参数里。",
  "",
  "1. 用户给了公开 https 网址且要进簿：`web_fetch`，再 `write_to_sheet` 写成新表。",
  "2. 需要登录或三方反爬站：不要问密码。告诉用户用「取数」栏打开本机 Chrome/Edge 跟手操作（验证码自己点）；在网页窗口点选同类或框选，写入按钮在网页上，不必回到 Excel。密码不进对话。",
  "3. 写入后可点「采集路径」查看 🟢🟡🔴 步骤；有 project 模板时提示用 reshape_table(project) 规整列。",
  "4. 不要编造数字。fetch 失败就标明未能获取。",
  "5. 用户其实在问「什么意思/怎么选口径/帮查资料」→ 说明这是调研，改走 /调研，不要用取数流程应付。",
].join("\n");

export const researchSkill = [
  "# 调研",
  "",
  "核实开放信息、对比口径、给摘要与引用。默认**不改工作簿**。不是把表抓进 Excel——那走 /取数。",
  "",
  "## 何时用",
  "",
  "用户问政策/费率/竞品/市场信号、要多个来源对照、要「帮我查一下再解释」、拆解里标 🔴 的外部核实。若用户明确要把公开表格**进簿**，改走 /取数。",
  "",
  "## 步骤",
  "",
  "1. 先澄清：要结论还是进簿？只要结论就留在对话里。",
  "2. 用 web_search（若可用）或用户给的公开 https + `web_fetch` **只读**正文；交叉核对至少两个来源，标 URL 与日期。",
  "3. 输出：事实（有来源）→ 可选口径 2–3 项 → 【逻辑推论】单独标注。不编行业基准、关税、广告费率。",
  "4. 登录才能看的站、需跟手点验证码的 ERP：标 🔴，让用户自取数栏或自行核实，不要问密码。",
  "5. 用户明确要求「整理成表/写进 Excel」时，才 `write_to_sheet` 小摘要表（几行要点），不要把整页 HTML 或 search JSON 贴进表。",
  "",
  "## 禁止",
  "",
  "- 把调研和取数合成一步（先搜再抓表再解读混在一个口令里）。",
  "- 用取数栏/扩展 picker 代替信息检索。",
  "- 把 web_search 的 encrypted_content / 原始 JSON 展示给用户。",
  "- 用户已上传内部文档时，可先用 search_knowledge；开放网仍用 web_search。",
].join("\n");

export const knowledgeSkill = [
  "# 知识",
  "",
  "检索用户上传到本机的私有文档（SOP、口径、费率说明）。默认**不改工作簿**。不是开放网调研（/调研）也不是表行进簿（/取数）。",
  "",
  "## 何时用",
  "",
  "用户问「按我们手册/内部规定/之前上传的文档」、清关 SOP、公司口径。文档需先在任务窗格「知」栏上传 .md/.txt/.csv。",
  "",
  "## 步骤",
  "",
  "1. `search_knowledge` 检索，引用 docName 与片段；无命中就说知识库没有，不要编政策。",
  "2. 可与 web_search 对照：内部为准还是公开网为准，列 2–3 个口径选项，不替用户拍板。",
  "3. 默认只在对话给结论；用户明确要求「整理成表」才 write_to_sheet 小摘要。",
  "",
  "## 禁止",
  "",
  "- 把知识库检索与取数栏抓表合成一步。",
  "- 没上传文档却假装检索到了内部规定。",
].join("\n");

export const deconstructSkill = [
  "# 拆解",
  "",
  "把用户以为一步做完的事，拆成可操作、可验证、能自动化的步骤。本回合不改工作簿。可 inspect 看真实表头。",
  "",
  "## 何时用",
  "",
  "用户说拆流程、能不能自动化、做成技能之前先拆、清关对账怎么落到表上、广告 ROI 怎么建。若只要安装技能且步骤已清楚，改去做技能，不要先甩斜杠清单。",
  "",
  "## 五步（先确认命名，再往下）",
  "",
  "1. 命名：外行能听懂的一句话。模糊就问：处理什么表、交给谁、什么事件触发。",
  "2. 动作流：按时间顺序，一行一个动词。每行必须有对象（哪张表/哪一列）、操作（哪个工具或菜单）、完成标志（看到什么算完）。禁止空话：检查、处理、整理、汇总、对齐、清洗、合并——出现就追问具体怎么做。目标大约 10–20 步。登录系统等不可再拆的，标注依赖界面。有当前工作簿时，动作里写真实表头。",
  "3. 判断点：只标开始做之前要选的事（口径、要不要排除测试单）。执行中的 #N/A、事后才发现的偏差放到验证，不算判断点。用户听了会说「废话」的删掉。",
  "4. 边界：开始/结束/输入/异常，各写理想和降级两版，必须是能看见的状态。",
  "5. 验证：至少 2 个锚点（对什么、应什么关系、偏差多大要复核）+ 3 个是/否/不确定问题（写清为什么问）。交叉检验三选一：另一算法、抽 5 行手算、让别人只看输入和产出。",
  "",
  "## 额外三块",
  "",
  "- 接管：每步 🟢 用户只给表且对错可判定 / 🟡 需要用户独有判断 / 🔴 必须跟人实时互动或靠没写下来的直觉。公式要调试标 🟡。不确定标 🟡 并写原因。",
  "- 🟢🟡 各给一段可复制提示词：目标、输入、正文。末尾提醒先完成验证再拿去跑。",
  "- 每个判断点一张是/否/不确定表，含「为什么问」；不确定要写去问谁或怎么查。",
  "",
  "## 禁止",
  "",
  "- 替用户选 GMV 口径、排除规则、异常阈值、统计周期、成本归属。只列 2–3 个选项和适用场合。",
  "- 编造节省时间、公式结果、行业基准。没有来源就写待验证或问用户。",
  "- 本回合不要 ensure_table、对账、写入、格式化。拆完后告诉用户：哪些 🟢 步骤可以做成技能。🟢 必须能映射到现有 Office JS 算子；映射不了的标 🟡/🔴，不要写成假工具名。外部信息核实、政策/竞品查资料标 🔴 或引导 /调研，不要并到 /取数。",
  "",
  "## 行业包（不进核心）",
  "",
  "清关、电商、SERP、利润模型等具体阈值与站点字段语义不要写进核心拆解正文。需要时引导：① /调研 或 /知识 ② 用户技能 ③ 仓库 samples/industry-deconstruct-appendix.md 上传知识库。拆解只映射现有算子。",
  "",
  "## 交接块（收尾必须输出，形状固定，供 /skill-creator 直接消费）",
  "",
  "最后用一个 markdown 代码围栏输出固定结构，围栏第一行写【拆解交接】。内容复用上面五步与三块的产出，浓缩成紧凑投影，不要另写一套：",
  "",
  "```【拆解交接】",
  "流程名：<一句话>",
  "🟢 步骤（每步映射真实算子）：",
  "- <动作一句话> → <算子名>（<输入表/列>）",
  "🟡 判断点（简表）：",
  "- <判断> → 选项：<2–3 项>",
  "验证锚点：",
  "- <对什么，应什么关系>",
  "```",
].join("\n");
