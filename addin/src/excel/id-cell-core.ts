/** Back-compat re-exports — prefer column-format-core. */
export {
  isIdLikeHeader,
  idLikeColumnIndexes,
  numberToPlainText,
  resolveIdCell,
  coerceRowIdCells,
  gridIdCellsAsText,
} from "./column-format-core";
