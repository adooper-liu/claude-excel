{
  "name": "web",
  "version": "1.0.0",
  "description": "Fetch a known URL via the local backend. Search is injected server-side on DeepSeek.",
  "tools": [
    {
      "name": "web_fetch",
      "description": "Fetch readable text of a known http(s) URL via the local backend. Never pass username or password. If the site needs login, tell the user to use the task pane 取数 bar (credentials stay on this machine). If fetch errors (Amazon often 403), mark 未能获取 — do not invent prices/ASINs.",
      "input_schema": {
        "type": "object",
        "properties": {
          "url": { "type": "string", "description": "Full http(s) URL" }
        },
        "required": ["url"]
      }
    }
  ]
}
