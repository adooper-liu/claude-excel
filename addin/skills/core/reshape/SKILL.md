# reshape

把脏表收成能算的新表。源表只读。

| op | 用户怎么说 | 结果 |
|---|---|---|
| `dedupe` | 按订单号去重 | 键相同只留第一行 |
| `unpivot` | 反透视 / 转长表 | 宽表变长表（属性 / 值） |
| `split` | 把标签按逗号拆开 | 一列拆成 `列_1` `列_2` … |
| `coerce` | 把金额转成数字 | 非法值变空 |

必须先是 Excel Table（`ensure_table`）。空工作簿或没有带表头的表时，先问要不要生成样例，不要报错结束。不跨文件合并。从网址取数走 /取数。
