# 2026-08-21

## 项目解读 + Gate 1b 深度分析（咨询，未改代码）

- 完成了全项目解读（三层架构：A 核心算力唯一写格 / B 核心数据配置 / C 用户扩展 Pack；addin React+Office JS，backend FastAPI :8765）。
- 发现工作区遗留状态：`docs/README.md` 已修改、`docs/glossary.md` 未跟踪；`origin/feat/finance-profit-formula` 远程分支存在（本地无）。
- 深度解读 Gate 1b（跨境电商业财闭环）：流程 = connector_load_feed → reconcile_tables(date_window ±7) → write_inputs(假设参数 B2–B4) → calculate_table SUMIFS → create_pivot → _pack_audit。
- **实用性缺口（重要结论）**：
  1. 净利公式未落地：profit_formula.md 有完整净利口径，但 finance-run.ts 只做 SUMIFS 订单金额汇总 + 透视，没有净利列/COGS/佣金/FBA 减项；profit-assumptions handler 的 DEFAULTS 未被前端消费。
  2. 假设参数不驱动重算：write_inputs 写 B2–B4 但 SUMIFS 不引用假设格，改假设结果不变。
  3. 退款未过滤：fixture 有 Refunded 负金额/PartialRefund 拆行，SUMIFS 直接加负金额。
  4. TACOS/ACoS 标注列与条件格式未实现；_pack_audit 是留痕不是把关（无匹配率警示）。
  5. 一致性：brief 说 pytest 用干净 5/4 行但 fixtures 已是 dirty 基准（52/40 行，brief 过时）；PACK_VERSION 硬编码；profit-assumptions DEFAULTS 与 profit_formula.md 默认值不一致。
- 给出 P0/P1/P2 改进建议（未执行）。

## Gate 1b 场景重拆：从财务原点出发（带信源，已落盘 docs/tasks/gate-1b-finance-origin-scenarios.md）

- 用户质疑「订单+广告」是研发预设，要求从财务原点拆真实场景 + 确认信源（拒绝 AI 虚幻）。
- **核心结论：财务原点 = 平台结算单（Settlement Report），不是订单也不是广告**。卖家每分钱从结算单进来：销售 − 佣金 − FBA − 仓储 − 广告 − 退款 − 其他 = 净额打款（多为 14 天周期）。
- 检索确认的信源：Amazon 官方结算文档（sellercentral-europe help 791 / G200801160，字段 settlement-id/deposit-date/total-amount）；FBA 2026 费率（佣金多数 15%、配送 $3.06–6.85、仓储 $0.87/$2.40 per cu ft、超龄 271 天起）；广告归因官方（SP 7 天 / SB 14 天末次点击，help G201530620）；amalyze：跨格式求和虚增 8–25%；德勤 2022：68% 对账差异来自汇率/服务费偏差；Gartner 2023：自动化对账 7 天→1.8 天；Statista：78% 品牌 2+ 渠道；Otterz：$50 商品到账 $37.42 示例。
- 六个真实场景（按付费意愿排序）：① 结算单拆解「钱去哪了」(P0) ② 单 SKU 真实净利 (P0) ③ 广告归因口径 (P1) ④ 现金流/结算周期 (P1) ⑤ 退款全成本（佣金只退 80%）(P1) ⑥ 多平台多币种 (P2)。
- 产品缺口：G1 settlement feed 缺失（最大）；G2 净利公式未落地（文档有、代码只 SUMIFS item_price）；G3 退款未过滤；G4 归因虚增警示；G5 匹配率把关；G6 费率单一真相（仓储费文档 $0.78 vs 官方 $0.87）；G7 汇率/汇损。
- 边界：全部落在 Pack 层 + finance-run.ts 编排层，不动核心算子。

## Gate 1b 自我批判（用户要求批判上轮产出，确保需求纯度与价值；已落盘 docs/tasks/gate-1b-self-critique.md）

- **信源实证（本轮重新检索验证）**：① S9 德勤 68% 严重失真——原语境是「固定汇率可降账务错误率 68%」，上轮写成「68% 差异源于汇率」，语义被改写（10100 SEO 百科转引）；② S1/S2 Amazon 官方帮助页需登录，从未开原文（字段细节仅搜索片段）；③ S8 amalyze 8-25% 属实但 feedvisor 给 20-40%、improvado 10-30%，多源区间不一，写死单值是精度幻觉；④ S12 $37.42 是营销博客示例非实证。
- **五刀批判**：① 信源失真（S9 最重，定纪律：A/B/C 三级标注，C 级降级或删）；② 原点演绎过度——结算单=现金实现（实收）不是唯一原点，闭环=订单(应收)/结算(实收)/广告(费用)三账本对齐+差异解释；Temu 供货制/Shopify 无平台结算单，范式不普适；③ 场景未收敛——6 场景实际只有 2 个独立（退款⊂净利、多币种⊂拆解、现金流数据不可得），「付费意愿排序」无实证；④ 商业定位矛盾——业财对账是积加/领星/马帮拳头功能且数据源在 ERP，正面打没赢面，Gate 1b 本职是技术门禁被误当商业场景；⑤ 卖点幻觉——「活调假设」是 Excel 基础能力，真差异=口径透明/异常把关/审计留痕。
- **修正后高纯度需求**：H1 单 SKU 净利（卖点=口径可调+假设驱动+审计）；H2 结算拆解（降为技术验证 feed）；H3 敏感性分析（新增主战场：退货率/汇率/佣金变化→净利变化，ERP 给不了）；价值主张一句话：用自然语言把任意口径的业财分析做出来·说清楚·改得动·可复核。
- 教训：给信源时必须开原文核对语义，转述失真比无来源更危险。

## 原 brief 按批判 §6 修订清单更新为 v2（docs/tasks/gate-1b-finance-origin-scenarios.md，覆盖写入）

- 按 gate-1b-self-critique.md §6 清单逐项落盘：① 原点表述改为「结算单=亚马逊 FBA 卖家现金实现锚点，闭环=订单/结算/广告三账本对齐」；② 场景 6→3（H1 净利/H2 结算拆解/H3 敏感性分析，删现金流、退款项并入 H1、多币种并入 H2）；③ 信源表全表 A/B/C 分级（S9 改写为降错率原语义、S10 删除、S3/S11 删数字、S8 改 8-40%、S12 标营销示例）；④ Gate 1b 定位=技术验证闭环，商业验证另立 H3；⑤ 缺口重排 G2→G5→G3→G1→G4/G6；⑥ 新增「信源 A/B/C 分级纪律」§7。
- 修订前后一致：全部改动仍在 Pack 层 + finance-run.ts，核心算子不动。待执行器按 §5 顺序开工。

## 价值主张 → 实施 brief（docs/tasks/gate-1b-value-implementation.md，新落盘）

- 用户要求把「在 Excel 里用大白话把账算清楚、说清楚、改得动、能复核」转成有价值实施。
- 一句话拆四个能力：算清楚=口径表（每个减项一行：中文标签+活公式+数值+来源）；改得动=参数区扩 12 字段（profit_formula.md §四）+ 全部减项引用参数格；能复核=假设快照入 _pack_audit + 匹配率<90% 标红 + 可下钻；说清楚=返回三段式人话结论（怎么算/哪些近似/哪些 SKU 有风险）。
- 关键设计约束：AI 结论只读 summary（recon.counts/SUMIFS 结果/审计行），表体不进模型上下文（架构铁律）。
- 实施顺序：活公式口径表（顺带 G6 单一真相）→ 把关升级 → AI 人话结论 →（可选）自然语言改参数。依赖确认项：calculate 是否支持跨表 SUMIFS 公式字符串、sheet 条件格式标色能力、write-inputs 是否支持公式前缀。

## Pack 化方案（用户选择「真正 Pack 化」，已落盘 docs/tasks/gate-1b-pack-ization.md）

- 代码调查结论：user-packs.md 设计本意 = SKILL.md 是编排层（Prompt-only 只调核心算子）；Gate 1b 三处偏离——① finance-run.ts 226 行行业 TS 预编排进核心 ② skill-handlers.ts run_flow flows 注册 finance 行业分支（违反「行业不进核心」禁令）③ isFinanceRequest 硬编码正则意图守卫。
- 机制盘点：编排所需算子（ensure_table/reconcile_tables/calculate_table/create_pivot/write_inputs/write_formula/conditional_format）全部在 HANDLED_TOOLS，value brief 三个依赖确认项其实已有工具支撑；SKILL.md prompt-only 注入机制已验证（product-info-search 遗留模式）；**唯一硬缺口 = appendPackAudit 无工具入口**（LLM 调不了审计写入）。
- 方案对比：推荐纯 LLM 编排（SKILL.md 强模板手册+删 TS），否决声明式 pipeline 引擎（违反「Pack 是组织层不是新机制」已定决策）和 run_flow 数据驱动（不彻底）。
- 落地：P0 append_pack_audit 注册为通用核心算子（唯一核心改动）→ P1 SKILL.md 重写（与 value-implementation **合并实施**避免二次返工：能力A/B/C 直接做进 SKILL.md 附录 A参数/B公式/C结论模板）→ P2 拆 TS（删 finance flow+finance-run+isFinanceRequest，测试迁移）→ P3 重验收（含：跑2次调用序列一致、卸载后场景消失、改模板重装零重编译）。
- 关键认知：公式字符串/参数默认值在 SKILL.md 里是**数据不是代码**——改口径=改文档重装 Pack，不发版加载项。

## 解读外部文档 Gate-1b-财务闭环-重新总结.md（Qoder 工作区输出）+ 发现执行器已实质开工

- 关键发现：**执行器已开工且 Pack 化 P0–P2 已落地**——`addin/src/excel/finance-run.ts` 已删除（P2）、`addin/skills/core/pack-audit/manifest.json` 新增（P0：append_pack_audit 通用核心算子，含 assumptionSnapshot/matchRate/sourceHash 字段）、`samples/packs/.../skills/finance-reconciliation/SKILL.md` 已重写为 8 步编排手册 + 附录 A（9 参数 B2–B10）/B（12 列减项公式全引用参数格）/C（三段式结论）、`knowledge/profit_formula.md` 已加「五、禁止项」6 条反幻觉铁律 + §四单一真相（G6 对齐）。工作区有大量未提交改动，git-flow check 待对齐。
- 文档对照结论：3 项一致（connector_load_feed 进簿、reconcile ±7 天、写格权/源表只读/反幻觉）、2 项偏差（文档说参数 12 项→实际 9 格；INDEX/MATCH+SUMIFS→实际纯 SUMIFS+write_formula；SHA-256→新版 sourceHash）、1 项失真（广告费「预付实时扣、欠费即停」→ 实为 2025-08-01 起仅对定向通知卖家：回款余额划扣优先、信用卡降备用、可申请发票月结 30 天）。
- 交叉发现：profit_formula.md B8 支付手续费率默认 2.5% 疑似偏高（收款工具实际 0.25%–1%，连连/PingPong），与文档资金流示例（$0.05–0.19）不一致，待执行器核对。
- 文档定位：对外业务叙事（非技术 spec），资金流四层叙事+时间差洞察（60–80 天资金占用）质量高，可作对外材料底稿；未提 Pack 化、H3 敏感性分析主战场、H2 settlement feed 计划。

## 修订对外版叙事（docs/gate-1b-external-narrative.md，新落盘）

- 按用户要求把 Qoder 版重新总结修订为「可用的对外版」，修正 + 补齐三项：① 信源 A/B/C 分级（正文标等级 + 附录 9 条清单：S4 归因窗口=唯一 A 级，S5 广告新规/S1 费率/S2 收款=B，资金流示例/痛点排序=C）；② Pack 化卖点（改口径=改文档重装 Pack 不发版，对外讲「不用等软件升级」）；③ H3 敏感性分析独立成节（主战场：ERP 答不了「如果…会怎样」，Excel 主场 + 自然语言改参数 + 批量敏感性矩阵 + 人话解释）。
- 同步修正三处偏差（参数 9 格 B2–B10、纯 SUMIFS+write_formula、sourceHash）与广告扣费新规表述（定向通知卖家、回款余额划扣优先、发票月结 30 天可申请，非「预付+立即暂停」）。
- 保留原文档高价值部分：资金流四层叙事 + 验算 + 时间差洞察、7 项诚实清单、与 ERP 三个真实差异点、一句话定位。
- 位置：docs/gate-1b-external-narrative.md（对外叙事材料，非 task brief）。
